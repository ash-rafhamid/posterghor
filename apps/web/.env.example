# Public URL of the API (the browser calls it directly; CORS on the API must allow this site's origin).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional: public origin of this site (used for absolute social-card URLs). On Vercel it is picked up automatically.
# NEXT_PUBLIC_SITE_URL=https://posterghor.example.com
